# Configure only as sensitive server-side environment variables in Vercel.
# Use the existing approved BNC Google Business Profile OAuth project.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
